{
  "name": "daily-spark",
  "version": "1.0.0",
  "private": true,
  "description": "A simple SEO-friendly static site with cached quotes via Vercel serverless functions.",
  "engines": {
    "node": ">=18"
  }
}
